#ifndef CLAUDE_USER_LIBC_STRINGS_H
#define CLAUDE_USER_LIBC_STRINGS_H

#include <stddef.h>

int strcasecmp(const char *a, const char *b);
int strncasecmp(const char *a, const char *b, size_t n);

#endif /* CLAUDE_USER_LIBC_STRINGS_H */
